name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      released: ${{ steps.semantic-release.outputs.new_release_published || 'false' }}
      version: ${{ steps.semantic-release.outputs.new_release_version }}
      tag: ${{ steps.semantic-release.outputs.new_release_git_tag }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        run: corepack enable

      - name: Install release tooling
        run: yarn install --immutable
        env:
          YARN_ENABLE_SCRIPTS: false

      - name: Semantic release
        id: semantic-release
        uses: cycjimmy/semantic-release-action@v4
        with:
          semantic_version: 24
          extra_plugins: |
            @semantic-release/commit-analyzer
            @semantic-release/release-notes-generator
            @semantic-release/github
            conventional-changelog-conventionalcommits
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}

  package-windows:
    runs-on: windows-latest
    needs: release
    if: needs.release.outputs.released == 'true'
    steps:
      - name: Checkout released commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release.outputs.tag }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: stable
          cache: true

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Build and smoke-test Windows package
        shell: pwsh
        run: .\scripts\package-flutter-windows.ps1 -Version '${{ needs.release.outputs.version }}'

      - name: Upload Windows package
        shell: pwsh
        run: |
          $archive = Get-Item -LiteralPath ".\release\ShowRunner-Flutter-windows-${{ needs.release.outputs.version }}.zip"
          gh release upload "${{ needs.release.outputs.tag }}" $archive.FullName --clobber
          gh release edit "${{ needs.release.outputs.tag }}" --draft=false --latest
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
