# Deterministic parity fixtures

These fixtures are small, stable product examples used by contract, runtime,
visual and end-to-end parity checks.

The fixture set intentionally contains only the active V2 data model. There is
no conversion fixture because the application does not import or rewrite old
document shapes.

Keep names, IDs, labels and numeric values stable. Tests and screenshots may
refer to them directly.
